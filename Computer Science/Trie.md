---
tags:
  - data_structure
---
The trie is a kind of [[Tree]], but unlike [[Binary Tree]]s, **trie nodes can have any number of child nodes**.

# Implementations
There are many ways to implement trie nodes, for example having each node hold a hash table of all child nodes, or a linked list or array, even.