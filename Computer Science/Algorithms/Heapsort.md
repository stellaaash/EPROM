---
tags:
  - algorithm
---
Heapsort is a simple algorithm that relies on a [[Binary Heap]]: **you put every element in a heap and then pop everything**.
The Heap Condition determines how the elements will be sorted when popped.